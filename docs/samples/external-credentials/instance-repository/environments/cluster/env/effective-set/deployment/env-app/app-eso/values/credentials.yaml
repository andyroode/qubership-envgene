---
# Local sensitive (creds.get); identical for vals vs eso (SECRET_MACRO_HANDLER applies only to external-credentials.yaml).
# This sample is external-only: no local credentials → file is empty.
{}
