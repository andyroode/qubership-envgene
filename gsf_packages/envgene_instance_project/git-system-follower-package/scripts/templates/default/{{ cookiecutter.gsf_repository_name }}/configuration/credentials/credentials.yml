# See full reference at https://github.com/Netcracker/qubership-envgene/blob/main/docs/envgene-objects.md#credential
#
# Self-token credential for EnvGene to commit changes to this repository
# Referenced in `/configuration/integration.yml`
self-token-cred:
  type: secret
  data:
    secret: "{{cookiecutter.self_token}}"
