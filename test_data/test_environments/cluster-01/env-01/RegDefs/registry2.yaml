name: registry2
credentialsId: registry2-credentials
mavenConfig:
    repositoryDomainName: maven.cluster01.example.com
    fullRepositoryUrl: 'https://maven2.example.com/repository'
    targetSnapshot: env01-snapshots
    targetStaging: staging
    targetRelease: releases
dockerConfig:
    snapshotUri: docker.cluster01.example.com/snapshot
    stagingUri: docker.cluster01.example.com/staging
    releaseUri: docker2.example.com/release
    groupUri: docker2.example.com/group
    snapshotRepoName: docker2-snapshot
    stagingRepoName: docker2-staging
    releaseRepoName: docker2-release
    groupName: docker2-group
helmConfig:
    snapshotUri: helm2.example.com/snapshot
    stagingUri: helm2.example.com/staging
    releaseUri: helm2.example.com/release
    groupUri: helm2.example.com/group
    snapshotRepoName: helm2-snapshot
    stagingRepoName: helm2-staging
    releaseRepoName: helm2-release
    groupName: helm2-group
