{
  "name": "{{ app_name | default('missing-fields-app') }}",
  "registryName": "{{ registry_name | default('missing-fields-registry') }}",
  "artifactId": "{{ artifact_id | default('com.example.missing') }}",
  "groupId": "{{ group_id | default('com.example') }}",
  "deployParameters": {
    "param1": "{{ deploy_param1 | default('missing-value1') }}",
    "param2": "{{ deploy_param2 | default('missing-value2') }}"
  }
  /* Missing required fields:
     - supportParallelDeploy
     - technicalConfigurationParameters
  */
}
