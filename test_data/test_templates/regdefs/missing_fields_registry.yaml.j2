# RegDef template with intentionally missing fields
name: {{ registry_name | default('missing-fields-registry') }}
credentialsId: {{ credentials_id | default('missing-fields-creds') }}

# Missing required fields:
# - mavenConfig
# - dockerConfig
